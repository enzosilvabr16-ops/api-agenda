spring:
  application:
    name: api-agenda

  datasource:
    url: jdbc:mysql://localhost:3307/bd-agenda?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true
    username: coti
    password: Coti2026
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
      host: localhost
      port: 5672
      username: coti
      password: coti
      virtual-host: /
      queue-name: notificacoes


server:
  port: 8083

cors:
  enabled: http://localhost:4200

jwt:
  secret: 73fe9e8d-1165-4178-b702-845dc8dfedb5
